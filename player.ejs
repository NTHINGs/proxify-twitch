<!doctype html>
<html>
    <head>
        <meta charset="utf-8">

        <title>ProxifyTwitch - <%=channel%></title>

        <meta name="description" content="">
        <meta name="viewport" content="width=device-width">
    </head>
    <body>
        <% for(var stream of streams) { %>
            <p><%=stream.link%></p>
        <% } %>
    </body>
</html>