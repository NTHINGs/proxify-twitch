<!doctype html>
<html>
    <head>
        <meta charset="utf-8">

        <title>ProxifyTwitch - <%=channel%></title>

        <meta name="description" content="">
        <meta name="viewport" content="width=device-width">
    </head>
    <body>
        <% for(var stream of streams) { %>
            <a href="/watch/<%=stream.url%>"><%=stream.quality%></p>
        <% } %>
    </body>
</html>